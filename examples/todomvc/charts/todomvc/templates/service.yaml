apiVersion: v1
kind: Service
metadata:
  name: {{ include "todomvc.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "todomvc.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
